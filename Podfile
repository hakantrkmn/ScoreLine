# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ScoreLine' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ScoreLine
  pod 'Alamofire'
  pod 'SDWebImage'
  pod 'IQKeyboardManagerSwift'
  pod 'SVProgressHUD'
  target 'ScoreLineTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ScoreLineUITests' do
    # Pods for testing
  end

end
